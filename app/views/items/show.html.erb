<% provide(:title, "Item Details: #{item_display_name @item}") %>

<main class="page-body">
  <%= render 'shared/heading' %>

  <%= render 'shared/details', object: @item %>

  <% if logged_in? && current_user.admin? %>
    <div class="dropdown option-dropdown">
      <a href="#" class="nav-link dropdown-toggle user-dropdown border d-inline btn" data-toggle="dropdown">
        Options
      </a>

      <ul class="dropdown-menu user-menu">
        <li>
          <%= link_to "Edit Item", edit_item_path %>
        </li>

        <li>
          <%= link_to "Delete", @item, method: :delete, data: { confirm: "You sure?" } %>
        </li>
      </ul>
    </div>
  <% end %>
</main>
