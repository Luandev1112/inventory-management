Hi <%= @user.name %>,

The available stock of the item <%= item_display_name @item %> is <%= @item.in_stock %> which is below the minimum required stock of the item (<%= @item.minimum_required_stock %>). You are adviced to take proper steps to check the quantity of the item.

Thanks,
Henlo IMS.
